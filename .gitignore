node_modules/
.comply/
.superpowers/
.turbo/
dist/

# A Seed and a run snapshot are generated, and neither ever belongs in git: a Seed
# is immutable and digest-named, so committing them means a new file on every
# extraction, and a recorded reading is a cache that regenerates from the two.
#
# Matched wherever they land rather than per shelf. Naming one shelf's paths is
# how 34MB of another repository's corpus reached a pull request: the shelf moves
# with COMPLY_SHELF, and a rule that has to be extended for each new one is a rule
# that is one shelf behind.
**/seeds/
**/runs/
